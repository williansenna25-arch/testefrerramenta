
O servidor estará rodando em `http://localhost:3000`

### Endpoints disponíveis

- POST `/api/gerar-presell`: gera página presell (envie JSON: modelo, link_afiliado)
- POST `/api/click`: registra clique
- POST `/api/webhook`: recebe webhook de conversão

---

## Frontend

### Instalação

1. Navegue até a pasta frontend:

